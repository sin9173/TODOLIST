spring:
  datasource:
    mysql:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: "jdbc:mysql://todo.nyangko.com:3306/?useSSL=false&allowPublicKeyRetrieval=true&serverTimzone=Asia/Seoul&allowMultiQueries=true&autoReconnection=true"
      username: sid
      password: dudwhg1!@
      minimum-idle: 10
      maximum-pool-size: 10
      test-while-idle: true
      validation-query: select 1
      idle-timeout: 60000